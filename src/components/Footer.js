import React from "react";

const Footer = (props) => (
    <div className="footer">
        <p>Made with React, Google Drawings, and &lt;3 by Jane  Kagan // (c) {props.year} </p>
    </div>
);

export default Footer;